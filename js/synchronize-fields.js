'use strict';
(function () {

  // var syncValues = function (element, value) {
  //   element.value = value;
  // };

  // window.synchronizeFields = function (element, value) {
  //   element.value = value;
  // };
})();
